\usepackage[a4paper]{geometry}

\geometry{
    top=1.8cm,
    bottom=1.8cm,
    left=2cm,
    right=2cm
}

% Typography

\usepackage{fontspec}

% Colors

\usepackage{xcolor}

% Layout

\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{parskip}
\usepackage{tabularx}
\usepackage{array}
\usepackage{hyperref}

\hypersetup{
    colorlinks=true,
    urlcolor=Accent,
    linkcolor=Accent
}

\pagestyle{empty}

\setlength{\parindent}{0pt}

\setlist[itemize]{
    leftmargin=*,
    itemsep=3pt
}